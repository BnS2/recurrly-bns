{
  "extends": ["stylelint-config-standard-scss"],
  "rules": {
    "scss/at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["theme", "utility", "tailwind", "apply", "variants", "responsive", "screen"]
      }
    ]
  }
}
